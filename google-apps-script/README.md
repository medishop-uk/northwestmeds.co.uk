# NorthwestMeds orders and reviews API

Bind this script to the Google Drive workbook `northwestmeds-data.xlsx`. Paste `reviews.gs` into Extensions > Apps Script, run `setup`, deploy it as a Web app, and put the deployment ID or full /exec URL into `../assets/js/config.js`.
